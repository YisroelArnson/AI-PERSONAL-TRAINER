# USER_PROFILE.md

Optional stable profile notes about the user.

V1 runtime note: the backend currently works best when these notes live inside `MEMORY`, rather than as a separate loaded document.

This can start empty.

Add stable facts as you learn them:
- identity and timezone
- goals
- training background
- constraints
- coaching preferences

If a separate file is not needed, keep these notes inside `MEMORY`.
